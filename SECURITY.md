# 安全与隐私

WakeTrace 会在后台调用模型，也可能连接个人工具。请把它视为拥有较高权限的私人服务。

## 默认安全边界

- HTTP 服务默认只监听 `127.0.0.1`；
- 除 `/health` 外，所有接口都需要管理员 Bearer Token；
- 模型请求失败时只保存简短错误类型，不保存完整响应正文和提示词；
- 工具结果进入数据库或模型上下文前会限制长度；
- Web Push 订阅只保存在本地 SQLite 数据库中；
- Web Push 端点返回 `404` 或 `410` 后会自动移除过期订阅；
- 跨进程租约会阻止两个进程同时执行同一次唤醒。
- 生活事件采用领取后确认语义，异常退出不会直接吞掉事件。

## 绝对不要提交

- `.env` 和任何 API Key；
- VAPID 私钥；
- Web Push 订阅 JSON；
- 生产环境 SQLite 数据库；
- 私人伴侣提示词；
- 聊天记录和私人行径；
- 会暴露私人基础设施的服务器地址。

## 生活事件与作品

`POST /world/events` 的 `summary` 和 `evidence` 会写入 SQLite，并可能出现在下一次模型请求中。
事件生产者应先做字段白名单、长度限制和必要的脱敏，不要把整个邮件、聊天记录、定位轨迹或访问令牌
直接作为证据传入。

线头与作品同样是持久化数据。当前接口只提供作品索引，不直接返回作品正文；备份和迁移数据库时仍应
把它们视作私人内容，并使用磁盘加密及严格的文件权限。

## 部署边界

不要把 FastAPI 服务直接暴露到互联网。如果确实需要远程访问，请在前面配置 HTTPS、身份验证、
请求大小限制、限流和访问白名单，并尽量让工具服务只存在于私有网络中。

## 工具设计

尽量注册功能单一、边界清楚的工具，只返回本次判断真正需要的字段。不要返回原始凭据、
完整私人文档或没有长度限制的日志。

成功的工具结果会成为可持久化证据，因此 `evidence_builder` 应采用保守摘要，避免把表达、猜测或
未经验证的模型结论包装成事实。

## 报告安全问题

请优先使用仓库 Security 页的私密漏洞报告入口，不要在公开 Issue 中披露尚未修复的漏洞。
如果暂时看不到私密报告入口，可以先创建一个不含漏洞细节的 Issue，请求仓库维护者建立私下联系。
